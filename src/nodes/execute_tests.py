"""execute_tests pipeline node — stub until Epic 4."""

from src.core.state import SataState


def execute_tests(state: SataState) -> SataState:
    """Stub: HTTP test execution with auth + basic retry logic."""
    return state
