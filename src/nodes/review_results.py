"""review_results pipeline node — stub until Epic 5."""

from src.core.state import SataState


def review_results(state: SataState) -> SataState:
    """Stub: Checkpoint 3 — re-test loop entry point."""
    return state
